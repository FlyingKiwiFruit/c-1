#include<stdio.h>
#define _CRT_SECURE_NO_WARNINGS 1
int main(void)
{
int i,n,number,sum;
printf("请输入整数的个数:\n");
scanf_s("%d", &n);
sum = 0;
printf("请输入%d个整数:\n", n);
for(i = 1; i <= n; i++)
	{
		scanf_s("%d", &number);
		sum += number;
}
printf("%d\n", sum);
return 0;
}
